#include "catch.hpp"
#include "CharBufferPrinter"

using namespace Catch::Matchers;

TEST_CASE("CharBufferPrinter tester", "[base]" ) {
    SECTION("Test CharBufferPrinter") {
        REQUIRE (false);
    }
}